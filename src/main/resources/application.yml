server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/resturant_system?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: '@Aa20050519'
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
      column-format: "`%s`"

# JWT 配置
jwt:
  secret: hotel2026!@#SuperSecretKeyForHS256Algorithm
  expire: 43200000  # 12小时，单位毫秒

